# Documentation authority

`docs/project-control/` is the current source of truth for product state,
decisions, permissions, backlog, risks, and task gates.

All other documents under `docs/` are design, implementation, qualification, or
historical evidence. They remain useful for provenance but do not grant current
authority. In particular, older paper-readiness, paper-run, serious-run,
acquisition, baseline-promotion, and next-task language is superseded wherever it
conflicts with `docs/project-control/`.

Current state:

- offline research/qualification only;
- no revised candidate selected;
- PULLBACK parked inconclusive;
- H1 and H2 parked after discovery failure;
- TIGHT_BASE isolated;
- holdout unopened;
- paper blocked and live prohibited;
- no current strategy-screen implementation ticket;
- further acquisition stopped absent the documented bounded exception.

Use `docs/project-control/RECOMMENDED_CHATGPT_SOURCE_PACK.md` for a compact
handoff. Do not bulk-upload `reports/`, `data/`, the full backlog, or the full
implementation log.
